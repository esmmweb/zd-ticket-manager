Hi Team,

A round of updates for your Pantheon site was staged and tested on a Multidev environment, autopilot, and is ready for your optional review.

If you do not reply to this message, these updates will be deployed on {{DEPLOY_DATE}}. Alternatively, we can:

- Pause the process and wait to deploy these updates. Please reply to this email by {{REPLY_BY_DATE}}, with "Please pause the Managed Updates process until further notice."
- Expedite the deployment. Please reply with "Please expedite the deployment as soon as possible"
- Schedule the deployment for a different date. Please reply with "Please schedule the deployment for (specify date)"

You can review the updated site here: {{SITE_REVIEW_URL}}

Visual Regression Testing of unauthenticated URLs passed, but no additional backend functionality was tested.

You can view the VRT report through this link: {{VRT_REPORT_URL}}

(Please note that the report will expire if any code is pushed to the Pantheon master branch before these updates.)

For your reference, here's a list of the updates performed and that are ready for deployment:

Core:
{{CORE_UPDATES}}

Modules/Plugins/Themes:
{{MODULE_UPDATES}}

Composer Dependencies:
{{COMPOSER_UPDATES}}

Here is a list of the URLs we tested; please let us know if you'd like to change these:
{{TESTED_URLS}}

What's next?

These updates were staged via Pantheon's Autopilot service but will be deployed via the MU Team's customized deployment framework, which includes multi-stage sandboxed VRT confirmation of deployment success for each environment in the stack. VRT results generated throughout the deployment process are reviewed by our team members to ensure clean deployment with no errors.

If we do not receive alternate guidance between now and the scheduled deployment, we will:

1. Merge the code into the DEV environment and re-run VRT testing. Note that this process will be stopped if we detect any uncommitted/undeployed code added to the master/main branch since we originally initiated the process. If VRT passes, we will proceed to the next step.
2. Deploy the updates to the TEST environment and re-run VRT testing. If VRT passes, we will proceed to the next step.
3. Initiate the creation of a restore point, and then proceed to deploy updated code to the LIVE environment of your site.

If any of the tests above fail, we will let you know about the next steps.

Please don't hesitate to reach out if you have any questions.
