<p>Hi Team,</p>

<p>A round of updates for your Pantheon site was staged and tested on a Multidev environment, <a href="{{AUTOPILOT_URL}}">autopilot</a>, and is ready for your optional review.</p>

<p>If you do not reply to this message, these updates will be deployed on {{DEPLOY_DATE}}. Alternatively, we can:</p>

<ul>
  <li>Pause the process and wait to deploy these updates. Please reply to this email by {{REPLY_BY_DATE}}, with "Please pause the Managed Updates process until further notice."</li>
  <li>Expedite the deployment. Please reply with "Please expedite the deployment as soon as possible"</li>
  <li>Schedule the deployment for a different date. Please reply with "Please schedule the deployment for (specify date)"</li>
</ul>

<p>You can review the updated <a href="{{SITE_REVIEW_URL}}">site here</a>.</p>

<p>Visual Regression Testing of unauthenticated URLs passed, but no additional backend functionality was tested.</p>

<p>You can view the VRT report through <a href="{{VRT_REPORT_URL}}">this link</a>.</p>

<p><em>(Please note that the report will expire if any code is pushed to the Pantheon master branch before these updates.)</em></p>

<p>For your reference, here's a list of the updates performed and that are ready for deployment:</p>

<p><strong>Core:</strong></p>
{{CORE_UPDATES}}

<p><strong>Modules/Plugins/Themes:</strong></p>
{{MODULE_UPDATES}}

<p><strong>Composer Dependencies:</strong></p>
{{COMPOSER_UPDATES}}

<p>Here is a list of the URLs we tested; please let us know if you'd like to change these:</p>
{{TESTED_URLS}}

<p><strong>What's next?</strong></p>

<p>These updates were staged via Pantheon's Autopilot service but will be deployed via the MU Team's customized deployment framework, which includes multi-stage sandboxed VRT confirmation of deployment success for each environment in the stack. VRT results generated throughout the deployment process are reviewed by our team members to ensure clean deployment with no errors.</p>

<p>If we do not receive alternate guidance between now and the scheduled deployment, we will:</p>

<ol>
  <li>Merge the code into the DEV environment and re-run VRT testing. Note that this process will be stopped if we detect any uncommitted/undeployed code added to the master/main branch since we originally initiated the process. If VRT passes, we will proceed to the next step.</li>
  <li>Deploy the updates to the TEST environment and re-run VRT testing. If VRT passes, we will proceed to the next step.</li>
  <li>Initiate the creation of a restore point, and then proceed to deploy updated code to the LIVE environment of your site.</li>
</ol>

<p>If any of the tests above fail, we will let you know about the next steps.</p>

<p>Please don't hesitate to reach out if you have any questions.</p>
